# SMPL-X models

Place SMPL-X model files here if you want to replace the approximate AMASS fallback with a full body-model frontend.

The v0.1 tests use canonicalized `.npz` files and do not require model assets.
